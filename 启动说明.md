# 医药订货系统 - 启动说明

## 🚀 两种启动方式

### 方式一：Docker Compose 启动（推荐）
适合生产环境或不想配置本地开发环境的场景

#### 前置要求
- 安装 [Docker Desktop](https://www.docker.com/products/docker-desktop/)
- 确保 Docker 已启动

#### 启动步骤
1. 双击运行 `start.bat`（Windows 批处理）
   或右键 `start.ps1` 选择"使用 PowerShell 运行"
   
2. 等待服务启动完成（首次需要 5-10 分钟）

3. 访问应用：
   - 前端: http://localhost:3000
   - 后端: http://localhost:8080

#### 停止服务
双击运行 `stop.bat`

---

### 方式二：本地开发环境启动
适合开发调试场景

#### 前置要求
- **JDK 17** 或更高版本
- **Node.js 18** 或更高版本
- Maven（项目自带 Maven Wrapper）

#### 启动步骤
1. 双击运行 `start-dev.bat`

2. 脚本会自动：
   - 启动后端服务（端口 8080）
   - 启动前端服务（端口 5173）

3. 访问应用：
   - 前端: http://localhost:5173
   - 后端: http://localhost:8080
   - H2 控制台: http://localhost:8080/h2-console

#### 停止服务
按启动窗口中的任意键

---

## 📝 脚本说明

| 文件名 | 说明 |
|--------|------|
| `start.bat` | Docker 启动（批处理） |
| `start.ps1` | Docker 启动（PowerShell） |
| `stop.bat` | 停止 Docker 服务 |
| `start-dev.bat` | 本地开发环境启动 |

---

## 🔧 常见问题

### Docker 启动失败
- 确保 Docker Desktop 已启动
- 检查端口 3306、6379、8080、3000 是否被占用
- 运行 `docker-compose logs` 查看详细日志

### 前端端口不是 3000
Vite 默认可能使用 5173 端口，检查启动日志中的实际端口

### H2 数据库连接
- JDBC URL: `jdbc:h2:mem:pharmacy_mall`
- 用户名: `sa`
- 密码: （留空）

---

## 📊 技术栈

| 组件 | 版本 |
|------|------|
| Spring Boot | 2.7.18 |
| Vue | 3.3.4 |
| MySQL（Docker） | 8.0 |
| Redis（Docker） | 7 |
| JDK | 17 |
| Node.js | 18+ |
